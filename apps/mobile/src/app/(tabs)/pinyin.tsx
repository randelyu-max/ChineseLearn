import { Text } from 'react-native';
import { Screen } from '@/components/ui';

export default function PinyinScreen() {
  return (
    <Screen>
      <Text accessibilityRole="header">拼音练习将在后续任务中实现。</Text>
    </Screen>
  );
}
