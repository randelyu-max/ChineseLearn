import { Text } from 'react-native';
import { Screen } from '@/components/ui';

export default function ReviewScreen() {
  return (
    <Screen>
      <Text accessibilityRole="header">复习计划将在后续任务中实现。</Text>
    </Screen>
  );
}
