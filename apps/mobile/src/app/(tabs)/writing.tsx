import { Text } from 'react-native';
import { Screen } from '@/components/ui';

export default function WritingScreen() {
  return (
    <Screen>
      <Text accessibilityRole="header">书写练习将在后续任务中实现。</Text>
    </Screen>
  );
}
