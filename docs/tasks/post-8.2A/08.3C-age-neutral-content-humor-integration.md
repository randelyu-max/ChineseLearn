# Task 8.3C — 13+ 内容与静态幽默接入正式 Session

**优先级：** P1
**依赖：** 8.3E。
**目标：** 让年龄中性课程和已审核幽默真正出现在正式 Session，而不是孤立 Fixture。
**非目标：** 不生成文本、不调用 AI。

## 开始前

- 读取根 `AGENTS.md`、Post-8.2A 补充设计、相关 ADR 和前置 Task 报告。
- 记录 `git status --short`、diff stat、分支、HEAD 和最近 5 个 commit。
- 不使用 `git reset --hard` 或 `git clean`。
- 不修改无关文件，不自动 push。
- 若前置 Task 未满足，停止并报告，不做临时绕过。

## 内容整改

- 移除/替换 `ageBand: 5-6` 等低龄元数据；
- 主题改为海外青少年和成人生活；
- 每关 1–2 个幽默点；
- 所有 humor 有 neutral fallback；
- 目标、答案、Evidence 不因幽默变化。

## Session 选择

在 Planner Materialization 时读取 Profile humor preference，使用纯选择器并固定：

- humor content ref
- package version
- rendered prompt/feedback

离线重放不因 Profile 后续变化而改变。

## 人工审核

任何内容改动产生新版本、审核人和时间。不得把自动验证等同人工语言审校。

## 测试

- off/light/playful；
- neutral fallback；
- 简繁目标一致；
- 同答案；
- 断网；
- 未审核拒绝；
- 13+ 文案审查；
- Session Snapshot 固定。

## 验收

正式课程中能看到适量、可关闭、不会改变学习结果的幽默，并且不再呈现幼儿化主题。

## 完成报告

必须列出：

1. 修改和新增文件；
2. 合同/算法/迁移版本；
3. 数据兼容与回滚；
4. 实际运行的命令；
5. 每项测试真实结果；
6. 安全、隐私、离线和学习影响；
7. 尚未解决的风险；
8. 下一项唯一建议 Task。

完成后停止，不自动开始下一 Task，不自动 push。
